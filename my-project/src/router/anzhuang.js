
//安装
  /*react-dom
    安装UI库  npm install antd-mobile --save

    按需加载#    
       npm install react-app-rewired --save-dev

       npm i babel-plugin-import -D
           使用 babel-plugin-import, babel-plugin-import
           是一个用于按需加载组件代码和样式的 babel 插件（原理），
           现在我们尝试安装它并修改 config-overrides.js 文件。

            然后只需从 antd-mobile 引入模块即可，无需单独引入样式
            babel-plugin-import 会帮助你加载 JS 和 CSS
            import { DatePicker } from 'antd-mobile';

       npm i react-scripts --save-dev

    把webpack的配置暴露出来：
       npm run eject
       简单配置，一个@符号代表src目录
        webpack.config.dev.js的 resolve的alias 中配置
          '@': paths.appSrc

        y使用react-app-rewire模块启动
  */
